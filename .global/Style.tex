% overall settings
\documentclass{article}

% pdf page geomentry settings
\usepackage{geometry}
\geometry{a4paper,left=2cm,right=2cm,top=2cm,bottom=2cm}

% set global font
\usepackage{fontspec}
\setmainfont[Ligatures=TeX]{Times New Roman}
\setsansfont[Ligatures=TeX]{Arial}

% define custom color
\usepackage{xcolor}
\definecolor{hwSolution}{RGB}{20,60,120}

% Ctex section format
\usepackage{titlesec}
    % SHALL BE DEFINED in compile.tex
    \def \SectionTitle{Section\_Title\_Undefined}
    \def \SubsectionTitle{Subsection\_Title\_Undefined}
    \def \SubsubsectionTitle{Subsubsection\_Title\_Undefined}

    \titleformat
        % titleformat TARGET
        {\section}
        % Format of the LABEL
        {\Large\bfseries}
        % Contents of the LABEL
        {}
        % Horizontal distence between LABEL and Title
        {0pt}
        %Contents AFTER LABEL
        {}
    \titleformat
        % titleformat TARGET
        {\subsection}
        % Format of the LABEL
        {\large\bfseries}
        % Contents of the labLABELel
        %{Notes on Lesson \arabic{subsection}}
        {\SubsectionTitle}
        % Horizontal distence between LABEL and Title
        {0pt}
        %Contents AFTER LABEL
        {}
    \titleformat
        % titleformat TARGET
        {\subsubsection}
        % Format of the LABEL
        {\normalsize\bfseries}
        % Contents of the labLABELel
        {\SubsubsectionTitle}
        % Horizontal distence between LABEL and Title
        {0pt}
        %Contents AFTER LABEL
        {}

% add space between equation lines
\addtolength{\jot}{8pt}

% Custom Definations
    % Add def of a section title with horizontial rule
    \def \SectionRule
    {
        \vspace{-15pt}\noindent\rule{\textwidth}{0.1pt}\vspace{-10pt}
    }
    % Add style specifications of appendix
    \def \cstAppendix#1{\sffamily{\small{\textnormal{\color{gray}#1}}}}
    \def \<{\left<}
    \def \>{\right>}
